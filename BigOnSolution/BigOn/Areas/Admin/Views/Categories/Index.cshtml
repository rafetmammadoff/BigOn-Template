@model IEnumerable<Category>
@{
    ViewData["Title"] = "Index";
}

<!-- Page-header start -->
<div class="page-header">
    <div class="row align-items-end">
        <div class="col-lg-8">
            <div class="page-header-title">
                <div class="d-inline">
                    <h4>Bootstrap Basic Tables</h4>
                    <span>
                        lorem ipsum dolor sit amet, consectetur adipisicing
                        elit
                    </span>
                </div>
            </div>
        </div>
        <div class="col-lg-4">
            <div class="page-header-breadcrumb">
                <ul class="breadcrumb-title">
                    <li class="breadcrumb-item" style="float: left;">
                        <a href="../index.html"> <i class="feather icon-home"></i> </a>
                    </li>
                    <li class="breadcrumb-item" style="float: left;">
                        <a href="#!">Bootstrap Table</a>
                    </li>
                    <li class="breadcrumb-item" style="float: left;">
                        <a href="#!">Basic Table</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>
<!-- Page-header end -->
<!-- Page-body start -->
<div class="page-body">
    <!-- Basic table card start -->
    <div class="card">
        <div class="card-header">
            <h5>Basic Table</h5>
            <span>use class <code>table</code> inside table element</span>

        </div>
        <div class="card-block table-border-style">
            <div class="table-responsive">
                <table class="table tree">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Created Date</th>
                            <th class="operation">
                                <a asp-action="create" class="btn btn-success">
                                    <i class="fas fa-plus nm"></i>
                                </a>
                            </th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var item in Model.Where(c=>c.ParentId==null))
                        {
                            <tr class="treegrid-@item.Id">
                                <td>@item.Name</td>
                                <td>@item.CreatedDate</td>
                                <td class="operation">
                                    <a asp-action="edit" asp-route-id="@item.Id" class="btn btn-warning">
                                        <i class="fas fa-pencil nm"></i>
                                    </a>
                                    <a asp-action="details" asp-route-id="@item.Id" class="btn btn-secondary">
                                        <i class="fas fa-eye nm"></i>
                                    </a>
                                    <a asp-action="delete" onclick="removeEntity(event,@item.Id,'@item.Name')" class="btn btn-danger">
                                        <i class="fas fa-trash nm"></i>
                                    </a>
                                </td>
                            </tr>

                            @foreach (var child in item.GetAllChildren())
                            {
                                <tr class="treegrid-@child.Id treegrid-parent-@child.ParentId">
                                    <td>@child.Name</td>
                                    <td>@child.CreatedDate</td>
                                    <td class="operation">
                                        <a asp-action="edit" asp-route-id="@child.Id" class="btn btn-warning">
                                            <i class="fas fa-pencil nm"></i>
                                        </a>
                                        <a asp-action="details" asp-route-id="@child.Id" class="btn btn-secondary">
                                            <i class="fas fa-eye nm"></i>
                                        </a>
                                        <a asp-action="delete" onclick="removeEntity(event,@child.Id,'@child.Name')" class="btn btn-danger">
                                            <i class="fas fa-trash nm"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <!-- Basic table card end -->

</div>
@section addcss{
    <link rel="stylesheet" href="~/libs/jquery-treegrid/css/jquery.treegrid.css" />
}
@section addJs{
    <script src="~/libs/sweetalert/sweetalert.min.js"></script>
    <script src="~/libs/jquery-treegrid/js/jquery.treegrid.js"></script>
    <script>
        $.fn.collapseAll=function(){
            $(this).find("tr").removeClass("treegrid-expanded").addClass("treegrid-collapsed");
            $(this).find("tr[class*='treegrid-parent-']").css("display", "none");
            $(this).find("tr span.treegrid-expander.treegrid-expander-expanded").removeClass("treegrid-expander-expanded")
                .addClass("treegrid-expander-collapsed");
        }
        $(document).ready(function(){
            $('.tree').treegrid();
            $('.tree').collapseAll();
        })
        function removeEntity(e, id, name) {
            e.preventDefault();

            swal({
                title: "Eminsen?",
                text: `${name} silinsinmi?`,
                icon: "warning",
                buttons: ["Xeyr", "Beli"],
                dangerMode: true,
            })
                .then((willDelete) => {
                    //if (willDelete) {
                    //    $.ajax({
                    //        url:`@Url.Action("Remove")/${id}`,
                    //        type:"POST",
                    //        success:function(response){

                    //            if (/[<>]+/g.test(response)){
                    //                $("#dynamic-body").html(response)
                    //            }
                    //            else{
                    //                toastr.error(response.message, "Xeta")
                    //            }
                    //        },
                    //        error:function(response){
                    //            toastr.error("Sistem xetasi", "Xeta")
                    //        }
                    //    })
                    //}
                    if (willDelete) {

                        $.ajax({
                            url: `@Url.Action("Remove")/${id}`,
                            type: "POST",
                            success: function (response, statusText, xhr) {

                                if (/[<>]+/g.test(xhr.responseText) == true
                                    || xhr.responseText.replace(/(\r|\n|\r\n|\s)/g, '').length == 0) {
                                    $('#dynamic-body').html(xhr.responseText);
                                }
                                else {
                                    toastr.error(response.message, "Xəta!");
                                }
                            },
                            error: function (response) {
                                toastr.error('Sistem xətası.Biraz sonra yenidən yoxlayın', "Xəta!")
                                console.warn(response);
                            }

                        });

                    }
                    else {

                    }
                });
        }
    </script>
    }